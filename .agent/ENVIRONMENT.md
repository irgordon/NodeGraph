# NodeGraph Agent Environment

This repository file is a neutral placeholder. NodeGraph creates an environment
report in the active graph workspace when a `.nodegraph.json` file opens.

The generated report describes the tools found on that computer. It must not be
used as a fixed requirement for another computer, operating system, shell, or
package manager.
